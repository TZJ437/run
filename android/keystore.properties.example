# 本地签名配置样板
# 使用步骤：
#   1. 用 keytool 生成一个 .jks（只做一次）：
#        keytool -genkeypair -v -keystore lightglass.jks -alias lightglass \
#          -keyalg RSA -keysize 2048 -validity 36500
#      按提示填写密码 + 你的姓名 / 组织 / 城市 / 国家即可。
#   2. 把 lightglass.jks 放到 android/ 目录下（已被 .gitignore 忽略，不会上传）。
#   3. 复制本文件为 android/keystore.properties（也被 gitignore 忽略）并填入真实值。
#   4. 运行 `cd android && ./gradlew assembleRelease`，产物在：
#        android/app/build/outputs/apk/release/app-release.apk
#
# ⚠️ keystore + 密码丢了就永远无法给旧用户发更新，务必备份！

storeFile=lightglass.jks
storePassword=请填你的 keystore 密码
keyAlias=lightglass
keyPassword=请填你的 key 密码
